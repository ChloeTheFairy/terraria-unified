--- src/TerrariaNetCore/Terraria/Graphics/Capture/CaptureInterface.cs
+++ src/Unified/Terraria/Graphics/Capture/CaptureInterface.cs
@@ -901,6 +_,9 @@
 	{
 		HoveredMode = -1;
 		bool flag = !Main.graphics.IsFullScreen;
+		// Fullscreen restriction removed as FNA doesn't use exclusive
+		// fullscreen.
+		flag = true;
 		int num = 9;
 		for (int i = 0; i < num; i++) {
 			if (!new Rectangle(24 + 46 * i, 24, 42, 42).Contains(mouse.ToPoint()))
@@ -1057,8 +_,10 @@
 						flag = true;
 					break;
 				case 7:
+					/*
 					if (Main.graphics.IsFullScreen)
 						flag = true;
+					*/
 					break;
 				case 5:
 					if (!Main.mapEnabled)
@@ -1112,8 +_,10 @@
 					text = text + "\n" + Lang.inter[112].Value;
 				break;
 			case 7:
+				/*
 				if (Main.graphics.IsFullScreen)
 					text = text + "\n" + Lang.inter[113].Value;
+				*/
 				break;
 			case 5:
 				if (!Main.mapEnabled)
